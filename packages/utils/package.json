{
  "name": "@acme/utils",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "exports": {
    "./redirection": "./src/redirection.ts",
    "./use-redirection": "./src/use-redirection.ts",
    "./lib/*": "./src/lib/*.ts"
  },
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "format": "ultracite format",
    "lint": "ultracite lint",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  },
  "devDependencies": {
    "@acme/tsconfig": "workspace:*",
    "@biomejs/biome": "catalog:",
    "typescript": "catalog:"
  }
}
