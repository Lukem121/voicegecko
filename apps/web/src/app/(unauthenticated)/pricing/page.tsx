

// Using nuqs We will check if the student Boolean query parameter is available and if so show the 50% off prices as students get 50% off their pro plan. ?student=true

export default function PricingPage() {
  return (
    <div>
      <h1>Pricing</h1>
    </div>
  );
}
