# Copy to apps/desktop/.env.development (gitignored) or set in repo root .env
# Dev scripts load ../../.env first, then this file (see package.json).

# Cloud GPT-4o transcription engines (Engine Lab + accuracy_cloud mode)
OPENAI_API_KEY=sk-your-key-here
